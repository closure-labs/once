# Once v0.2 acceptance matrix

| Requirement | Evidence | Status |
| --- | --- | --- |
| Separate authoritative policy repository | [`closure-labs/once-policy`](https://github.com/closure-labs/once-policy) | Implemented |
| Protected policy `main` branch | Active ruleset `Protect policy main` | Implemented |
| Full immutable GitHub commit required | Nix adapter unit and CLI integration tests | Implemented |
| Nix metadata revision independently checked | Revision-match and mismatch tests | Implemented |
| Mutable branch/tag reference rejected | CLI fail-closed integration test | Implemented |
| Tiny `#policy` artifact read through Nix | Template flake check and real consumer test | Implemented |
| Local TOML retained for development | Existing CLI integration tests | Implemented |
| Protected invocation pinned in Once CI | `.github/workflows/ci.yml` bootstrap step | Implemented |
| Reusable GitHub Action | Composite Action, wrapper tests, and CI smoke test | Implemented |
| Distinct diagnostic command output | Purpose-built v1 views and CLI tests | Implemented |
| JSON compatibility suite | Issue #5 | Pending |
| Safe remote trace validation | Issue #6 | Pending upstream feasibility |

Statuses are complete only when automated or externally verifiable evidence
exists. The protected policy does not change the v0.1 remote-trace limitation.
